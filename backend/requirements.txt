# ── Web framework ───────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
# ── Async / jobs ────────────────────────────
celery==5.4.0
redis==5.0.4
# ── Banco / Supabase ────────────────────────
supabase==2.4.6
psycopg2-binary==2.9.9
# ── Validação ───────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1
# ── HTTP client ─────────────────────────────
httpx==0.27.0
aiohttp==3.9.5
# ── Storage (R2 / S3 compatível) ────────────
boto3==1.34.84
# ── Pagamentos ──────────────────────────────
stripe==9.9.0
# ── APIs de IA ──────────────────────────────
anthropic==0.28.0
openai==1.30.1
elevenlabs==1.1.2
# ── Imagem (usado no Instagram Dark — gera a faixa/molde) ──
Pillow==10.4.0
# ── Vídeo (usado no Facebook Dark — resolve link em vídeo baixável, roda local sem API terceira) ──
yt-dlp
# ── Utilitários ─────────────────────────────
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
